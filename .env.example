# Environment Variables
NODE_ENV=development
PORT=5000

# ===========================================
# DATABASE CONFIGURATION
# ===========================================
# Choose ONE of the following methods:
# - If DATABASE_URL is set, it takes priority (for remote/production databases)
# - If DATABASE_URL is NOT set, individual DB_* parameters are used (for local development)

# METHOD 1: Remote/Production Database (using DATABASE_URL)
# Uncomment the line below to use your remote Neon database
# DATABASE_URL=
# DB_SSL=true

# METHOD 2: Local/Staging Database (using individual parameters)
# Comment out DATABASE_URL above and use these for local development
DB_HOST=
DB_PORT=
DB_NAME=
DB_USER=
DB_PASSWORD=

# JWT Configuration
JWT_SECRET=ecopilot_secret_key_change_in_production_2025
JWT_EXPIRE=7d

# CORS
CORS_ORIGIN=http://localhost:5173
